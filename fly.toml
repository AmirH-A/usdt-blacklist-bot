app = 'usdt-blacklist-bot'
primary_region = 'iad'

[build]

[env]
  DB_PATH = "/data/blacklist_tracker.db"

[mounts]
  source = "bot_data"
  destination = "/data"

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
